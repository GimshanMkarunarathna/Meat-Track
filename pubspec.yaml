name: meat_track
description: A new Flutter project for meat inventory tracking.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0

  # Me package eka aniwareyenma thiyenna oni data refresh wenna
  provider: ^6.1.1

  # Cuperitno icons (Apple style icons)
  cupertino_icons: ^1.0.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  